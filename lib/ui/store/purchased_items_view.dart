import 'package:flutter/material.dart';

class PurchasedItemsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Purchased View"),
      ),
    );
  }
}
